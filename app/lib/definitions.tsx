interface UserData {
    id: bigint;              // Используем bigint для ID
    first_name: string;      // Используем string для текста
    balance: number;         // Используем number для real
    deposit: number;         // Используем number для real
    accumulation: number;    // Используем number для real
  }
  